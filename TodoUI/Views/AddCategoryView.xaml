<UserControl x:Class="TodoUI.Views.AddCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TodoUI.Views"
             mc:Ignorable="d" Background="White" FontSize="20"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <!-- Row 0 -->
            <TextBlock Grid.Column="0" VerticalAlignment="Center">Category name</TextBlock>
            <TextBox Grid.Row="0" x:Name="CategoryName" Grid.Column="1"></TextBox>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="CreateCategory" Margin="1 0" Padding="5">+</Button>
               <Button x:Name="CancelCreation" Margin="1 0" Padding="5">X</Button>
        </StackPanel>

    </Grid>
</UserControl>
